generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Campus {
  id        String   @id @default(uuid())
  name      String
  boundary  Json
  createdAt DateTime @default(now())
}